# Dev environment credentials — copy to .env.dev and fill in values
POSTGRES_USER=jobmatcher
POSTGRES_PASSWORD=changeme_dev
POSTGRES_DB=jobmatcher_dev
# REQUIRED — app will refuse to start if missing or set to a "changeme" placeholder.
# Stable secret key for Flask session signing; a new random key on every restart
# invalidates all session cookies and breaks CSRF protection.
# Generate once with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=changeme_generate_with_python_secrets_token_hex_32
